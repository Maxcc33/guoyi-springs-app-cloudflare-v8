# wrangler.toml - Cloudflare Pages 配置

compatibility_date = "2026-03-17"           # 必须，建议当前或未来日期
compatibility_flags = ["nodejs_compat"]      # 启用 crypto 等 Node.js 模块

# D1 数据库绑定（保持不变）
[[d1_databases]]
binding = "DB"
database_name = "guoyi-messages"
database_id = "a5482efb-0aea-4f9a-bfe5-152e2b408716"
